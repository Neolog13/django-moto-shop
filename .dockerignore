# Исключаем Python-байткод и кэш
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Миграции можно исключить, если они генерируются автоматически (по желанию)
# migrations/

# Исключаем виртуальные окружения, если они есть в проекте
venv/
env/

# Логи и временные файлы
*.log
*.tmp
*.bak
*.swp

# Исключаем IDE-файлы
.vscode/
.idea/

# Git-репозиторий
.git/
.gitignore

# Файлы, не нужные в образе
*.sqlite3
*.env

# Исключаем ненужные статические файлы (если генерируются отдельно)
staticfiles/
media/# Исключаем Git-репозиторий
.git
.gitignore

# Python байткод и кэш
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Среда виртуального окружения (если вдруг она в проекте)
venv/
env/

# Файлы IDE
.vscode/
.idea/
*.swp
*.swo

# Логи
*.log

# Тестовые и временные файлы
*.tmp
*.bak
*.orig

# Фикстуры и базы, которые не нужны в образе (если они не используются)
#*.sqlite3

# Docker и CI/CD файлы (если не нужны в образе)
#docker-compose.yml
#Dockerfile
#entrypoint.sh  # Удаляй из игнора, если он нужен в контейнере

# Медиаданные (если не нужны в образе)
#media/
#staticfiles/  # если используется collectstatic в CI/CD